using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeatUnit : MonoBehaviour
{
    public float distanceToExit; 
    //create a float clamped from 0 to 1 named heat
    [Range(0, 1)]
    public float heatMapValue;
    //create a float clamped from 0 to 1 named heatChange
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
